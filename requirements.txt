# Core dependencies
requests>=2.28.0

# Audio transcription (requires ffmpeg on system)
faster-whisper>=1.0.0

# PostgreSQL integration (for query_db.py and hub_adapter.py)
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
financial-hub-postgres @ git+https://github.com/LeXrLt/financial_hub_postgres.git
